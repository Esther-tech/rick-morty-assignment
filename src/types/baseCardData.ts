export type BaseCardData = {
  id: number;
  name: string;
  subtitle?: string; // bijv. species, dimension, episode code
  description?: string; // bijv. status, air_date
  image?: string; // optioneel
};
